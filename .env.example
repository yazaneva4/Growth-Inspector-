# ── Supabase ───────────────────────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Service role — server only, bypasses RLS for webhook ingestion. Also
# enables the sign-in self-heal for accounts stuck unconfirmed (see DEPLOY.md).
# From Supabase dashboard -> Project Settings -> API -> service_role. Keep secret.
SUPABASE_SERVICE_ROLE_KEY=

# ── Anthropic (the AI brain) ───────────────────────────────
ANTHROPIC_API_KEY=

# ── Google Gemini (free-tier fallback + Growth Agent) ──────
# Used by the Growth Operator when Gemini is selected.
GEMINI_API_KEY=

# ── GPT / OpenAI (Growth Operator model option) ────────────
# Server-only key. The Growth Operator uses the OpenAI Responses API when
# GPT is selected. OPENAI_MODEL is optional; the current default is gpt-5.6.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6

# ── z.ai / Zhipu GLM (first fallback after Claude) ─────────
ZAI_API_KEY=
ZAI_MODEL=glm-4.6

# ── OpenRouter (two responder fallback tiers) ──────────────
OPENROUTER_API_KEY=
OPENROUTER_MODEL_A=openai/gpt-oss-120b:free
OPENROUTER_MODEL_B=google/gemma-4-31b-it:free

# ── Meta / WhatsApp & Instagram (webhooks + sending) ───────
META_VERIFY_TOKEN=
META_ACCESS_TOKEN=
META_APP_SECRET=

# ── Outbound email (invoices, access approvals, inbox replies) ──
GMAIL_USER=
GMAIL_APP_PASSWORD=
EMAIL_FROM=Growth Inspector <support@yourdomain.sa>

# Google sign-in
NEXT_PUBLIC_GOOGLE_ENABLED=

# ── Voice calls (Twilio) ───────────────────────────────────
TWILIO_AUTH_TOKEN=
TWILIO_ACCOUNT_SID=

# ── Voice transcription upgrade (OpenAI Whisper) ───────────
OPENAI_API_KEY=
